# Environment for `make run`. Mimics what HeLx injects at launch so that a
# local container is served from the same kind of prefix as a real one.
USER=userff
NB_PREFIX=/private/ohif-viewer/userff/0123456789abcdef0123456789abcdef
